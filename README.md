# VulFix-RAG
VulFix-RAG is a knowledge-augmented vulnerability repair framework that combines AST-based static analysis,ChromaDB semantic retrieval,and DeepSeek LLMs to detect &amp; repair software security vulnerabilities. It identifies vulnerable code, retrieves relevant security guidance, generates secure patches, and validates the generated fixes automatically.
